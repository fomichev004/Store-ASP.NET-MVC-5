@model IEnumerable<my_store_project.Models.ViewModel.Shop.CategoryVM>

@foreach (var item in Model)
{
    <a class="list-group-item list-group-item-action" href="/shop/category/@item.Slug.ToLower()">@item.Name</a>
}